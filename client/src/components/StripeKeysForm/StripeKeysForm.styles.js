import { styled } from '@mui/material/styles';
import { Box } from '@mui/material';

export const StripeKeysFormStyles = styled(Box)(({ theme }) => ({
  maxWidth: 600,
  margin: '0 auto',
  marginTop: theme.spacing(3),
}));

