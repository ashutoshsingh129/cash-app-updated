import { styled } from '@mui/material/styles';
import { Box } from '@mui/material';

export const PaymentFormStyles = styled(Box)(({ theme }) => ({
  maxWidth: 600,
  margin: '0 auto',
  padding: theme.spacing(3),
}));

